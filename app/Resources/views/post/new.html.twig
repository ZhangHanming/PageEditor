{% extends 'base.html.twig' %}

{% block body %}
    <h1>Post creation</h1>

    {% if error %}
        <h1>{{ error }}</h1>
    {% endif %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('post_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
